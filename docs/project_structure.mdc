---
description: 
globs: 
alwaysApply: false
---
# Структура проекта MathIDE

## ✅ Модульная архитектура (после рефакторинга)

**Статус:** Рефакторинг завершен! Проект разделен на 13 специализированных модулей.

## Основные директории

| Папка/файл         | Назначение                                                        |
|-------------------|------------------------------------------------------------------|
| core/             | Ядро системы: модульная логика генерации и применения преобразований |
| interfaces/       | Реализации пользовательских интерфейсов (CLI, Telegram, Web)       |
| prompts/          | Промпты для GPT и шаблоны сообщений                               |
| tests/            | Юнит-тесты и интеграционные тесты                                 |
| docs/             | Документация, PRD, планы, инструкции                              |
| utils/            | Утилитарные модули (математика, логирование)                      |
| .cursor/rules/    | Специфичные для Cursor правила и описания                         |
| .venv/            | Виртуальное окружение Python (создаётся автоматически)            |

## 🧠 Ядро системы (core/) - После рефакторинга

| Файл                | Размер        | Назначение                                           |
|--------------------|---------------|------------------------------------------------------|
| engine.py          | 701 строка    | Основная бизнес-логика (сокращено с 1003 строк)     |
| history.py         | 247 строк     | Управление историей шагов (без изменений)           |
| gpt_client.py      | 175 строк     | Абстракция над OpenAI API (новый модуль)           |
| types.py           | 128 строк     | Все dataclass'ы и enum'ы (новый модуль)            |
| parsers.py         | 120 строк     | Парсинг JSON/LaTeX ответов (новый модуль)          |
| exceptions.py      | 89 строк      | Иерархия исключений (новый модуль)                 |
| __init__.py        | 80 строк      | Реэкспорт основных типов (новый модуль)            |
| prompts.py         | 51 строка     | Управление промптами (новый модуль)                 |

**Итого ядра:** 1591 строка в 8 модулях (было 1003 строки в 1 файле)

## 🖥️ Интерфейсы (interfaces/) - Обновленные размеры

| Файл                | Размер        | Назначение                                           |
|--------------------|---------------|------------------------------------------------------|
| cli.py             | 238 строк     | CLI-интерфейс (без изменений)                       |
| telegram_bot.py    | 63 строки     | Главный модуль бота (сокращено с 1357 строк)        |
| __main__.py        | 53 строки     | Точка входа приложения                              |
| __init__.py        | 8 строк       | Инициализация пакета                                |

## 📱 Telegram бот (interfaces/telegram/) - Новая модульная структура

| Файл                | Размер        | Назначение                                           |
|--------------------|---------------|------------------------------------------------------|
| handlers.py        | 267 строк     | Обработчики команд и callback'ов (новый модуль)     |
| renderers.py       | 242 строки    | Рендеринг LaTeX → изображения (новый модуль)       |
| rate_limiter.py    | 122 строки    | Ограничение запросов (новый модуль)                |
| utils.py           | 82 строки     | Утилиты отправки сообщений (новый модуль)          |
| keyboards.py       | 80 строк      | Inline-клавиатуры (новый модуль)                   |
| state.py           | 58 строк      | Управление состоянием пользователей (новый модуль)  |
| __init__.py        | 42 строки     | Инициализация модуля (новый модуль)                |

**Итого Telegram:** 893 строки в 7 модулях (было 1357 строк в 1 файле)

## 🛠️ Утилиты (utils/) - Новые модули

| Файл                | Размер        | Назначение                                           |
|--------------------|---------------|------------------------------------------------------|
| math_utils.py      | 88 строк      | Математические функции (новый модуль)               |
| logging_utils.py   | 44 строки     | Настройка логирования (новый модуль)                |
| __init__.py        | 4 строки      | Инициализация модуля (новый модуль)                 |

**Итого утилит:** 136 строк в 3 модулях (новая функциональность)

## 📝 Промпты GPT

| Файл                      | Назначение                                               | Статус реализации |
|--------------------------|----------------------------------------------------------|------------------|
| prompts/generation.md    | Генерация списка допустимых преобразований               | ✅ Реализован     |
| prompts/apply.md         | Применение выбранного преобразования                     | ✅ Реализован     |
| prompts/check.md         | Проверка завершённости решения                           | ✅ Реализован     |
| prompts/progress_analysis.md | Анализ прогресса и рекомендации возврата к прошлым шагам | ✅ Реализован     |
| prompts/verification.md  | Проверка и пересчёт математических преобразований        | ✅ Реализован     |
| prompts/transformation_types.md | Справочник типов преобразований                          | ✅ Реализован     |

## 🧪 Тестирование (tests/)

| Файл                | Назначение                                               |
|--------------------|----------------------------------------------------------|
| test_engine.py     | Тесты ядра системы                                       |

*Дополнительные тесты для новых модулей планируются к добавлению*

## ⚙️ Конфигурационные файлы

| Файл                      | Назначение                                               | Git статус |
|--------------------------|----------------------------------------------------------|------------|
| .env                     | Основной файл с переменными окружения                     | игнорируется |
| .env.local               | Локальные переопределения переменных окружения            | игнорируется |
| docs/env.example         | Пример файла с переменными окружения                      | отслеживается |
| pyproject.toml          | Конфигурация poetry и зависимостей                        | отслеживается |
| .gitignore              | Список игнорируемых git файлов                            | отслеживается |

## 📊 Статистика рефакторинга

### До рефакторинга (монолитная архитектура):
- `core/engine.py`: **1003 строки**
- `interfaces/telegram_bot.py`: **1357 строк**
- **Итого основных файлов**: 2360 строк в 2 монолитах

### После рефакторинга (модульная архитектура):
- **Ядро**: 1591 строка в 8 модулях
- **Telegram бот**: 893 строки в 7 модулях  
- **Утилиты**: 136 строк в 3 модулях
- **Интерфейсы**: 362 строки в 4 модулях
- **ИТОГО**: ~2982 строки в 22 модулях

### Ключевые достижения:
- ✅ **95% сокращение** главного файла Telegram бота (1357 → 63 строки)
- ✅ **30% сокращение** основного движка (1003 → 701 строка)
- ✅ **13 новых специализированных модулей** созданы
- ✅ **Принцип единой ответственности** соблюдается во всех модулях
- ✅ **Средний размер модуля**: 50-250 строк (вместо 1000+)

## 🏗️ Архитектурные компоненты

### Ядро системы (core/)
**TransformationEngine** в `engine.py` включает методы:
- `generate_transformations()` - генерация преобразований
- `apply_transformation()` - применение преобразований  
- `check_solution_completeness()` - проверка завершённости
- `analyze_progress()` - анализ прогресса решения
- `verify_transformation()` - проверка корректности преобразований

**Вспомогательные модули:**
- `GPTClient` - единая точка взаимодействия с AI
- `PromptManager` - управление промптами из .md файлов
- Строго типизированные модели данных в `types.py`
- Иерархия исключений в `exceptions.py`

### Интерфейсы (interfaces/)
- **CLI** (`cli.py`) - интерфейс командной строки с поддержкой предпоказа
- **Telegram бот** - модульная архитектура:
  - Обработчики команд (`handlers.py`)
  - Рендеринг LaTeX → PNG (`renderers.py`) 
  - Rate limiting (`rate_limiter.py`)
  - Управление состоянием (`state.py`)
  - Inline-клавиатуры (`keyboards.py`)
  - Утилиты сообщений (`utils.py`)

### Тестирование (tests/)
- Юнит-тесты ядра системы
- Интеграционные тесты (планируются)

## 🔧 Виртуальное окружение

Проект использует poetry для управления зависимостями:
- Виртуальное окружение создаётся в папке `.venv`
- Все зависимости описаны в `pyproject.toml`
- Установка: `poetry install`
- Активация: `poetry shell`

## 🌍 Переменные окружения

Порядок загрузки (от высшего приоритета):
1. `.env.local` (локальные переопределения, не в git)
2. `.env` (основной файл, не в git)  
3. `docs/env.example` (пример конфигурации, в git)
4. Системные переменные окружения

## 📂 Генерация новых файлов

Новые файлы следует создавать в следующих директориях:
- `core/` - модули ядра системы
- `interfaces/` - пользовательские интерфейсы
- `interfaces/telegram/` - компоненты Telegram бота
- `utils/` - утилитарные модули
- `prompts/` - промпты для GPT
- `tests/` - тесты

## 🎯 Преимущества новой архитектуры

1. **Читаемость**: каждый модуль имеет четкое назначение
2. **Тестируемость**: модули можно тестировать изолированно  
3. **Масштабируемость**: легко добавлять новые интерфейсы
4. **Поддерживаемость**: изменения локализованы в конкретных модулях
5. **Переиспользование**: утилиты доступны всем компонентам

---

**Последнее обновление:** Декабрь 2024 - завершен рефакторинг в модульную архитектуру
